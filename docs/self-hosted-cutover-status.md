26 августа 2026: после смены SMTP-пароля аутентификация SMTP прошла, реальный OTP успешно доставлен на Gmail пользователя (попал в «Спам»). При открытии `/login` на VPS и повторном запросе того же адреса приложение корректно показало ограничение «Подождите минуту перед повторной отправкой» после недавней отправки; это штатный rate limit, а не ошибка SMTP. Код в чат не выводился.
После прямого успешного API-запроса OTP на Gmail была выполнена браузерная проверка `/login` по IP. Email введён, кнопка «Получить код» нажата после истечения минутного лимита. Форма кратко показала «Отправляем...», затем вернулась к исходному состоянию без экрана ввода кода; в журнале systemd за последние минуты явной строки `[Email]` не обнаружено. Прямой API smoke ранее подтвердил доставку, поэтому UI-переход к экрану ввода кода требует отдельной диагностики до DNS cutover.
Повторная ротация через ISPmanager была выполнена без вывода нового значения, но первый сгенерированный пароль содержал shell-символы и временно сделал `/opt/posle/smtp.conf` некорректным: приложение не запустилось штатно, а попытка SMTP verify вернула `535 Invalid login or password`. Секрет не записывается в документацию. Для восстановления выбран новый пароль только из alphanumeric-набора; текущая форма ISPmanager оставлена открытой для повторного сохранения.
После повторного входа в ISPmanager пароль технического ящика был заменён через штатную форму способом без вывода значения в browser/tool output. Секрет передан на VPS временным файлом с правами 600, файл удалён после применения. `posle.service` перезапущен, активен; `node scripts/verify-smtp.mjs` завершился `SMTP authentication verified`. Сам пароль нигде в документацию не записывается.
## 2026-08-26 — DNS/HTTPS cutover

- A-записи `posle-grooming.ru` и `www.posle-grooming.ru` переведены на `89.111.152.205` через ISPmanager DNS management.
- MX-записи сохранены: `mx1.hosting.reg.ru` priority 10 и `mx2.hosting.reg.ru` priority 20.
- Let’s Encrypt сертификат выпущен и подключён к Nginx для обоих имён; HTTP → HTTPS redirect проверен, оба HTTPS-запроса возвращают 200.
- Публичный DNS через Google DoH возвращает `89.111.152.205` для apex и `www`.
- Smoke публичных маршрутов `/`, `/about`, `/results`, `/contacts`, `/login`, `/booking`, `/account`, `/admin`, `/master` через HTTPS возвращает 200.
- После cutover остаются отдельные проверки авторизованных клиентского/admin/master/YClients-flow; доступ к трём историческим legacy media-объектам всё ещё отсутствует в исходном Forge.

Секреты, OTP-коды и пароли в этот журнал не записываются.

## 2026-08-26 — admin upload/storage backup smoke

Проверка через production HTTPS-домен завершилась успешно: `POST /api/upload` вернул `200`, новый кабинетный файл создан в `/var/lib/posle/uploads`, защищённое чтение администратором вернуло `200`, а файл присутствовал в свежем `/var/backups/posle/daily/*/uploads.tar.gz`. Временный smoke-файл после проверки удалён. Три исторических legacy media-объекта с исходного Forge по-прежнему не считаются восстановленными.
